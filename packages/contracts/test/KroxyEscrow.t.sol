// SPDX-License-Identifier: MIT
pragma solidity ^0.8.24;

import {Test} from "forge-std/Test.sol";

/// @dev Placeholder test suite — real contract tests live here once
///      KroxyEscrow.sol is added to src/.
contract KroxyEscrowTest is Test {
    function test_placeholder() public pure {
        // CI gate: forge pipeline is healthy
        assertTrue(true);
    }
}
